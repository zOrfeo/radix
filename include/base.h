#ifndef FORMAT_H
#define FORMAT_H

enum class Base {
    BINARY,
    OCTAL,
    DECIMAL,
    HEXADECIMAL,
    UNKNOWN
};

enum class BasePrefix {
    BINARY,
    OCTAL,
    DECIMAL,
    HEXADECIMAL,
    UNKNOWN,
    NONE
};

#endif
